<!-- Logo and Title -->
<div class="row">
    <div class="col-12">
        <img class="logo" src="/assets/images/burger.jpg" alt="burger">
        <h1 class="title">Eat a Burger!</h1>
    </div>
</div>

<!-- Form to Submit Burger Names -->
<div class="row">
    <div class="col-12">
        <form class= "create-form">
            <div class="form-group">
                <label for="burgerName">Enter a burger name to add it to the menu:</label>
                <input id="burgerName"type="text" name="name">
            </div>
            <button type="submit">Add to Menu</button>
        </form>
    </div>
</div>

<div class="row">
    <!-- Burger Menu -->
    <div class="col-12 col-md-6">
        <div class="card">
            <div class="card-header">On the Menu</div>
            <div class="card-body">
                <ul>
                    {{#each burgers}}
                    {{#if devoured}}
                    {{> burgers/burger-block devoured=false}}
                    {{/if}}
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>

    <!-- Devoured Burgers List -->
    <div class="col-12 col-md-6">
        <div class="card devoured-card">
            <div class="card-header devoured-header">Devoured Burgers</div>
            <div class="card-body">
                <ul>
                    {{#each burgers}}
                    {{#unless devoured}}
                    {{> burgers/burger-block devoured=true}}
                    {{/unless}}
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
</div>